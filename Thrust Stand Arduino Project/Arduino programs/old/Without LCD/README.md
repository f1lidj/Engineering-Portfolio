# Early Firmware — Serial Measurement

This section contains an earlier stage of the **Arduino propulsion test-stand** development before the local LCD interface was introduced.

## My Work

I used the simpler serial-based architecture to establish the core load-cell acquisition, calibration and static-test sequence before adding a richer local user interface and dual-channel operating workflow.
